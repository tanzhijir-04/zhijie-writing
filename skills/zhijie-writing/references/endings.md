# Endings

Default closure:

`return to opening object/question → state mechanism plainly → state key condition/limit → optional implication`

Use a broader social or future implication only after the core answer is closed. An open ending is valid when the evidence genuinely leaves a consequential uncertainty. Do not introduce a new central claim in the last block.
